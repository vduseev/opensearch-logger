language: python

python:
  - "2.7"
  - "3.6"

addons:
  apt:
    packages:
      - default-jre

services:
  - elasticsearch

install:
  - pip install tox-travis
  - pip install codecov

before_script:
  - sleep 10

script: tox

after_success:
  - codecov

notifications:
  email: false
